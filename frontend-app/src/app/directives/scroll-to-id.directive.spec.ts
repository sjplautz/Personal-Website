import { ScrollToIdDirective } from './scroll-to-id.directive';

describe('ScrollToIdDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollToIdDirective();
    expect(directive).toBeTruthy();
  });
});

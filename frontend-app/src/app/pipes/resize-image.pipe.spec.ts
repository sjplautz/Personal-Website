import { ResizeImagePipe } from './resize-image.pipe';

describe('ResizeImagePipe', () => {
  it('create an instance', () => {
    const pipe = new ResizeImagePipe();
    expect(pipe).toBeTruthy();
  });
});
